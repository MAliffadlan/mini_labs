---
import Layout from '../layouts/Layout.astro';
import ToolPageShell from '../components/ToolPageShell.tsx';
import CodePlaygroundApp from '../apps/dev/CodePlayground.tsx';

const pageTitle = "Code Playground";
const pageDescription = "Write HTML, CSS, and JavaScript with a VS Code-style editor and see live preview results instantly.";
const icon = "👨‍💻";
const color = "#8b5cf6";

const suggestions = [
  { title: 'JSON Formatter', icon: '{}', href: '/json', color: '#3b82f6' },
  { title: 'CSS Formatter', icon: '💅', href: '/css-formatter', color: '#3b82f6' },
  { title: 'Regex Tester', icon: '.*', href: '/regex', color: '#10b981' }
];
---

<Layout title={pageTitle} description={pageDescription}>
  <main style="padding: 2rem 1rem; max-width: 1400px; margin: 0 auto;">
    <ToolPageShell
      client:load
      toolTitle={pageTitle}
      toolIcon={icon}
      toolHref="/code-playground"
      toolColor={color}
      suggestions={suggestions}
    >
      <div style="margin-bottom: 2rem;">
        <h1 style="font-size: 2.5rem; font-family: 'Outfit', sans-serif; font-weight: 700; color: #fff; margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.75rem;">
          <span style={`color: ${color}`}>{icon}</span> {pageTitle}
        </h1>
        <p style="color: var(--text-secondary); font-size: 1.125rem; max-width: 600px;">
          {pageDescription}
        </p>
      </div>

      <CodePlaygroundApp client:only="react" />
    </ToolPageShell>
  </main>
</Layout>
